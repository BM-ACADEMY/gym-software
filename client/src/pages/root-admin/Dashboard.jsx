import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Dashboard = () => (
  <ModulePlaceholder title="Platform Dashboard" description="Platform overview: MRR, ARR, total subscribers, active vs inactive, trials in progress, churned this month, new signups (7/30 day). AI: churn-risk flag on subscribers trending toward non-renewal." />
);

export default Dashboard;
