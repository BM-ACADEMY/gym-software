import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Settings = () => (
  <ModulePlaceholder title="Gym Settings" description="Gym profile, branding (logo/colors for the member-facing app), working hours, holiday calendar, staff & member ID format." />
);

export default Settings;
