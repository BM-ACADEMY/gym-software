import { useState } from 'react';import { Copy, Dumbbell, MoreVertical, Plus, Users } from 'lucide-react';import { Button, Card, Field, Modal, Page, PageHeader, Pill, SearchBox } from './ui';import { workouts as seed } from './data';
const Workout=()=>{const [q,setQ]=useState('');const [open,setOpen]=useState(false);const [plans,setPlans]=useState(seed);const [name,setName]=useState('');const add=()=>{if(name)setPlans([...plans,{id:Date.now(),name,level:'Beginner',days:3,members:0,focus:'General fitness',updated:'Just now'}]);setName('');setOpen(false)};return <Page><PageHeader title="Workout plans" description="Build reusable programs and assign them to your members." actions={<Button onClick={()=>setOpen(true)}><Plus className="h-4 w-4"/>Create workout</Button>}/><SearchBox value={q} onChange={setQ} placeholder="Search workout plans..."/><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{plans.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())).map(w=><Card key={w.id} className="overflow-hidden"><div className="bg-gradient-to-br from-gray-900 to-teal-900 p-5 text-white"><div className="flex justify-between"><div className="rounded-xl bg-white/10 p-2"><Dumbbell className="h-5 w-5"/></div><button><MoreVertical className="h-5 w-5"/></button></div><h3 className="mt-6 text-lg font-bold">{w.name}</h3><p className="mt-1 text-sm text-teal-100">{w.focus}</p></div><div className="p-5"><div className="flex gap-2"><Pill tone="blue">{w.level}</Pill><Pill>{w.days} days/week</Pill></div><div className="mt-5 flex items-center justify-between border-t border-gray-100 pt-4"><span className="flex items-center gap-2 text-sm text-gray-500"><Users className="h-4 w-4"/>{w.members} members</span><button className="flex items-center gap-1 text-sm font-semibold text-teal-600"><Copy className="h-4 w-4"/>Duplicate</button></div></div></Card>)}</div><Modal open={open} title="Create workout plan" onClose={()=>setOpen(false)} footer={<><Button variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={add}>Create plan</Button></>}><div className="space-y-4"><Field label="Plan name" value={name} onChange={e=>setName(e.target.value)} placeholder="e.g. Beginner strength"/><div className="grid grid-cols-2 gap-3"><label className="text-sm font-medium">Level<select className="mt-1.5 w-full rounded-xl border border-gray-200 p-2.5"><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></label><Field label="Days per week" type="number" defaultValue="3"/></div><Field label="Primary focus" placeholder="Strength, mobility, fat loss..."/></div></Modal></Page>};export default Workout;
