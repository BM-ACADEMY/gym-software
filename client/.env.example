## Base URL of the deployed backend API (Railway/Render), including the /api
## suffix. Falls back to http://localhost:5000/api for local dev if unset.
VITE_API_URL=https://your-backend.up.railway.app/api
