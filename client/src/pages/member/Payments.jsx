import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Payments = () => (
  <ModulePlaceholder title="Payments" description="Pay online (UPI/card) for renewal, view/download invoices, payment history." />
);

export default Payments;
