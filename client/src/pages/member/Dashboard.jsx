import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Dashboard = () => (
  <ModulePlaceholder title="My Dashboard" description="Plan status (days remaining), next PT session, this week's attendance streak, quick check-in QR." />
);

export default Dashboard;
