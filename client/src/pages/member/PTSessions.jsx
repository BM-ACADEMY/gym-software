import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const PTSessions = () => (
  <ModulePlaceholder title="PT Sessions" description="View/request PT session slots with your assigned trainer, cancel/reschedule within the policy window." />
);

export default PTSessions;
