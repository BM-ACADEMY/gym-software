import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Subscribers = () => (
  <ModulePlaceholder title="Subscriber Management" description="List/search/filter all gyms by plan, status, signup date. Drill into a gym's usage stats, staff count, member count and payment history. Suspend / activate / delete, plus audited impersonation for support." />
);

export default Subscribers;
