import ModulePlaceholder from '../../components/ui/ModulePlaceholder';

const Settings = () => (
  <ModulePlaceholder title="Settings" description="Profile, password, notification preferences, linked payment method." />
);

export default Settings;
